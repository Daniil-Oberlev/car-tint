import { NavigationMenuDemo } from "@/components/layout/nav";

export const Header = () => {
  return (
    <header className="shadow flex items-center">
      <NavigationMenuDemo />
    </header>
  );
};
